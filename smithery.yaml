name: agent-integration-with-mcp-servers
version: "1.0.0"
description: "Diagnostic minimal Smithery config"

type: stdio
startCommand: python3 app.py

env:
  OPENAI_API_KEY: ${OPENAI_API_KEY}
  NOTION_TOKEN: ${NOTION_TOKEN}
  GITHUB_TOKEN: ${GITHUB_TOKEN}

permissions:
  network: true
  filesystem: false
